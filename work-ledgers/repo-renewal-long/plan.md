# Plan

1. Load the work command reference and mandatory repository policy files.
2. Inspect references to `.agents/`, `.claude/`, `.factory/`, `prior local-overlay agent-copy directory/`, and `prior local-overlay noise before deletion.
3. Remove explicitly authorized legacy directories and local runner package/dependency artifacts.
4. Update root docs, local runner overlay docs, harness cleanup notes, and durable ledger files.
5. Validate with requested git commands.
6. Run independent review and Qwen/GLM judges.
7. Report concise evidence and leave handoff for coordinating process.
