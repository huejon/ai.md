# Plan

1. Load the work command reference and mandatory repository policy files.
2. Inspect references to `.agents/`, `.claude/`, `.factory/`, `.opencode/agents/`, and `.opencode/` noise before deletion.
3. Remove explicitly authorized legacy directories and OpenCode package/dependency artifacts.
4. Update root docs, OpenCode overlay docs, harness cleanup notes, and durable ledger files.
5. Validate with requested git commands.
6. Run independent review and Qwen/GLM judges.
7. Report concise evidence and leave handoff for Hermes.
