# Memory Candidates

- This repository's OpenCode-specific policy is expected to live at `.opencode/AGENTS.md` and states there is no human review gate for routine maintenance; OpenCode/Hermes may commit and push directly to `main` after validation unless active task instructions forbid it.
- Root `CLAUDE.md` was intentionally removed on 2026-06-30 as part of deeper cleanup; do not recreate it unless explicitly requested.
- `.opencode/` should stay limited to project policy/readme/config/command files and durable active work ledgers unless a future task explicitly authorizes more. Legacy local prompt-copy directories and vendored dependency artifacts should not be reintroduced without evidence.
