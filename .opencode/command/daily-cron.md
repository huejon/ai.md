---
description: Continue the durable Hermes/OpenCode repository renewal and long-horizon improvement harness.
agent: work
---

You are continuing the durable repository renewal harness for this Hermes/OpenCode operations repo.

Arguments: $ARGUMENTS

Follow `cron-harness/README.md` exactly and continue the durable ledger at `.opencode/works/repo-renewal-long/` unless the user explicitly names a different active work ledger.

1. Complete repository startup: read `AGENTS.md`, `README.md`, `knowledge/INDEX.md`, and `cron-harness/README.md`; run `git log --oneline -10`; run `date`; inspect the relevant tree.
2. Load or create the active work ledger and update `state.md`, `plan.md`, `run-log.md`, `decisions.md`, `memory-candidates.md`, and `handoff.md` as needed.
3. Select one narrow renewal objective that improves recurring long-task operation, removes proven noise, or strengthens commands/skills/harness evidence.
4. Apply safe, reversible local edits; deletion is allowed only when the active task or user instruction explicitly authorizes it and evidence is recorded first.
5. Use `cron-harness/evaluation-checklist.md` before reporting completion.
6. Record findings, cleanup decisions, reviews, proposals, and handoff under `.opencode/works/repo-renewal-long/`.

Report files changed, files created, files removed, verification commands with directories and exit codes, and remaining risks.
